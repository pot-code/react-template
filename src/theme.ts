const theme = {
  colors: {
    primary: "#1890ff",
    secondary: "#404258",
  },
  size: {
    sm: 4,
    md: 8,
    lg: 16,
    xl: 32,
    xxl: 64,
  },
};

export default theme;

export type Theme = typeof theme;
